@(
  Layout = "_Layout";
)

<h2>Is it a square?</h2>

<p>Your rectangle's length is @Model["ResultingRectangle"].GetLength()</p>
<p>Your rectangle's width is @Model["ResultingRectangle"].GetWith()</p>

@if (@Model["ResultingRectangle"].IsSquare());
{
  <h4>Your rectangle is a square!</h4>
  <p>Surface area of a cube with those dimensions: @Model["ResultingCube"].GetSurfaceArea()</p>
  <p>Volume of a cube with those dimensions: @Model["ResultingCube"].GetVolume()</p>
}
else
{
  <h4>Your rectangle isnt a square!</h4>
}

<p><a href="/">Make another rectangle!</a></p>
